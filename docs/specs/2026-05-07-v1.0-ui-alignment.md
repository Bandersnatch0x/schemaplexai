---
topic: ui-alignment
stage: standard
change_id: v1-ui-alignment
status: approved
created_at: 2026-05-07
version: 1.0
---

# Spec: Frontend UI/UE Alignment

## Overview

Complete frontend UI/UE alignment for SchemaPlexAI: restructure routing with two layout modes, integrate Hive design system components, replace all mock/placeholder data with real API calls, add global error recovery, and ensure consistent empty-state handling across all pages.

**Scope**: `schemaplexai-ui/src/` — 19 files, +572 / -468 lines.

---

## 1. Router Restructuring

**File**: `src/router/index.tsx`

Split routes into two layout modes:

| Layout | Routes | Sidebar | Header |
|--------|--------|---------|--------|
| `ImmersiveLayout` | `/cockpit`, `/canvas` | 52px icon-only | Floating overlay |
| `ProgressiveLayout` | `/agents/*`, `/workflows`, `/specs`, `/contexts`, `/quality`, `/integrations`, `/ops`, `/notifications`, `/settings` | 200px with labels | 48px fixed |

- `/` and `/dashboard` redirect to `/cockpit`
- All authenticated routes wrapped in `<RequireAuth>` (checks `schemaplexai_token` in localStorage)
- `*` route renders `<NotFound />`

---

## 2. Cockpit Page

**Files**: `src/pages/Cockpit/index.tsx`, `src/pages/Cockpit/Cockpit.css`

- Remove inline HexIcon/StatCard definitions (80+ lines)
- Import `HexIcon`, `StatCard` from `@/components/Hive`
- Extract all inline styles to `Cockpit.css` (157 lines)
- Maintain API integration with `getAgentStats()` and 30s polling
- Add offline disconnection notice when API unreachable

---

## 3. AgentCanvas DAG Editor

**File**: `src/pages/AgentCanvas/index.tsx`

- Extract `TopologyView`, `ListView`, `CodeView` as independent top-level components (avoid re-creation on each render)
- Replace `nodes.find((n) => n.id === conn.from)` with `nodeMap.get(conn.from)` for O(1) lookup
- Replace `Date.now()` with `crypto.randomUUID()` for collision-safe node ID generation
- Add `locale={{ emptyText: t('common.noData') }}` to ListView Table
- Fix `ListViewProps.listColumns` type using `ColumnsType<CanvasNode>` from antd

---

## 4. AgentDetail Page

**File**: `src/pages/AgentDetail/index.tsx`

Refactored into identity card + tabbed layout:
- **Identity Card**: Avatar, name, ID, version, status badge, LLM model badge
- **Tabs**: Metrics / Logs / Charts (placeholder) / Config
- **Metrics Tab**: Execution metrics (success rate, latency, runs) + resource usage (tokens, cost, cache hit)
- **Logs Tab**: TerminalLog component from Hive
- **Config Tab**: JSON dump of agent configuration
- API integration via `getAgentDetail(id)`

---

## 5. WorkflowMonitor Page

**File**: `src/pages/WorkflowMonitor/index.tsx`

- Remove all mock data (`MOCK_RUNS`, hardcoded `HOURS` array)
- Dynamic timeline generation via `buildTimelineMeta(runs)`:
  - Computes start/end hours from actual API data
  - Generates hour labels dynamically
  - Falls back to 09:00-12:00 when no data
- Gantt-style bar positioning based on `timelineMeta.start` / `timelineMeta.duration`
- PillNav filter tabs (all/running/completed/failed) with status counts
- API integration via `getWorkflowList()` + `getWorkflowInstances()`
- Offline notice when API unreachable

---

## 6. Dashboard Page

**File**: `src/pages/Dashboard/index.tsx`

- Replace hardcoded 7-day chart data with `useMemo` aggregation from API records
- Extract date keys from `records[].createdAt`, count per day, sort chronologically
- Add `locale={{ emptyText }}` to Table

---

## 7. Global ErrorBoundary

**Files**: `src/components/ErrorBoundary/index.tsx`, `src/components/ErrorBoundary/ErrorBoundary.css`

- Class component with `getDerivedStateFromError` + `componentDidCatch`
- Abyss Hive themed error page: icon, title, description, error trace in monospace block
- Actions: "Reload Page" (primary) + "Go Home" (ghost)
- Wrapped around entire `<App>` in `App.tsx`

---

## 8. Empty State Handling

All Ant Design `<Table>` components across pages receive `locale={{ emptyText: t('common.noData') }}`:

- AgentManager
- ContextCenter
- Dashboard
- IntegrationCenter
- OpsCenter
- QualityCenter
- SpecCenter
- WorkflowCenter

---

## 9. Test Fixes

- `src/api/__tests__/request.test.ts`: Remove unused `MockInstance` import
- `src/stores/__tests__/userStore.test.ts`: Add missing `vi` import
- `src/components/__tests__/TenantSelector.test.tsx`: Remove unused `tenants` variable declarations from 2 test cases

---

## 10. App.tsx Cleanup

- Wrap app with `<ErrorBoundary>`
- Extract `<AppFallback>` component (centered Spin with Abyss background)
- Remove hardcoded Chinese "加载中..." text
- Create `App.css` for fallback styling

---

## Quality Gates

| Gate | Result |
|------|--------|
| TypeScript (`tsc --noEmit`) | Zero errors |
| Tests (`vitest run`) | 91/91 pass |
| Build (`vite build`) | 31.64s success |
| No MOCK_*/DEMO_*/SAMPLE_* | Verified |
| No console.log in production | Verified |
| No `any` types in pages | Verified |

---

## Out of Scope

- Login page decorative telemetry counters (hardcoded `agents: 247`) — cosmetic, not data integrity
- AgentDetail ChartsTab — feature gap placeholder, not alignment issue
- ESLint configuration — pre-existing project config issue (no `.eslintrc` file)
